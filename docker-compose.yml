version: '3.8'

services:
  web:
    build: .
    # command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "80"]
    # command: ["fastapi", "dev", app.main:app"]
    volumes:
      - .:/app
    ports:
      - "8000:8000"

# volumes:
#   pgdata:
#     driver: local
